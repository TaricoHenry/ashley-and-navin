rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cloud Functions (Admin SDK) bypasses Firestore rules.
    // Since your site ONLY uses Cloud Functions, deny ALL client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}